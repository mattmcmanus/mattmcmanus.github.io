---
const { note, class } = Astro.props;
---
<article class="mb-24 h-entry {class}" role="article">
  <!-- {% if note.bookmark-of and note.title != '' %}
    <h4>Link: <a class="u-bookmark-of h-cite" href="{{ note.bookmark-of }}">
      {{ note.title}}
    </a></h4>
  {% endif %} -->

  <div class="entry-content e-content">{ note.content }</div>
  <p class="-mt-4">
    <a href={note.url} class="no-underline text-grey-dark">
      <time class="text-grey-dark text-xs uppercase dt-published" datetime={ note.frontmatter.date }>
        { note.date }
      </time>
    </a>
    - <a href={note.url} class="u-url no-underline text-grey-dark text-xxs uppercase">Permanent Link</a>
    <!-- {%- for tag in note.tags -%}
      {{ tag }}
    {%- endfor -%} -->
  </p>
</article>
